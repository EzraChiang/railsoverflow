<h1>Rails Overflow</h1>

<% @questions.each do |question| %>
  <%= link_to "#{question.title}", question_path(question) %>
  <h5>author: <%= question.user.username %></h5>
  <p><%= question.created_at %></p>
<% end %>
